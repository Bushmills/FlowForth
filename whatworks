#--- tested ---
vm:		hi level code nesting
		colon definitions
		constant
		literal
stacks:		dup drop over swap ?dup 2dup 2drop depth nip tuck
		>r r@ r> rdrop
memory:		@ ! +! here allot ,
arithmetics:	+ - * / negate abs 1+ 1- min max int */ /mod
compare:	< 0= 0< = <> > >= <=
logic:		and or xor invert lshift rshift
flow control:	begin again
		begin until
		begin while repeat
		if then
		if else then
		for next
		bye
		execute
		exit ?exit
i/o:		led . type	( should be type$ - outputs and drops top string stack item )


#--- not yet tested ---
rdepth

#--- missing ---
all unsigned arithmetic and comparison
comparison:
arithmetic:
